# Django Settings
SECRET_KEY=development-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# For Docker: Use DB_HOST=127.0.0.1 (or localhost)
# These variables are used by both Django and docker-compose.yml
# IMPORTANT: Do NOT use DB_USER=root - use a different username like 'usuario'
DB_ENGINE=django.db.backends.mysql
DB_NAME=BDVENDAS
DB_USER=usuario
DB_PASSWORD=123
DB_HOST=127.0.0.1
DB_PORT=3307
